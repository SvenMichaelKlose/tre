(section
	(title "Introduction")
	(para
		"TRE is a programming language which revolves around the most"
		"beloved data structure in information technology: the tree.")

	(para
		"TRE's highlights are"
		(ul
			(li "it's a compiled language and mostly written in itself")
			(li "can use third party C compilers for code generation")
			(li "utilise C and C++ libraries (gccxml required)"))

		(ul
			(li "identifiers are case-sensitive")
			(li "namespace tree: namespaces can be nested")
			(li "terse syntax"))))
