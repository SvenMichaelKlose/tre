(section
	(title "Symbols")

	(para
    	"Any single character or sequence of characters without whitespaces or"
    	"special characters is grouped to a 'symbol'. Every time a new symbol is"
    	"read, it is added to the symbol table.")

	(para
    	"A symbol may refer to any object, including itself. Symbols"
    	"referring to other objects are called 'variables'.")

	(para
    	"Function names are separated from names of all other objects."))
