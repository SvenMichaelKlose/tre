DEFSTRUCT: MAKE-*: Keywords don't set value if it has the name of a function.

interpreter/MACROEXPAND-1: - CAR required?

(setf this.parent)

Find illegal keyword arguments.

Segmentation fault * (mapcar #'builtinp *universe*)
Segmentation fault * (assoc key rec #'bug)

Literal characters as argument default values.
